overwrite: true
schema:
  - "${GATSBY_API_URL}":
      headers:
        Authorization: "Bearer ${API_TOKEN}"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
